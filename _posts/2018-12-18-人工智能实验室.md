---
layout:     post                           # 使用的布局（不需要改）
title:      人工智能实验室                   # 标题
subtitle:   类神经元人工智能实验              #副标题
date:       2018-12-18                      # 时间
author:     小虫子                           # 作者
header-img: img/post-bg-debug.png            #这篇文章标题背景图片
catalog: false                               # 是否归档
tags:                                        #标签
    - yii
---

php语言实现描述：
```
/**

1.初始化1亿个神经元对象，并建立好输入输出联系
2.从输入集合输入数据
3.每个收到数据的神经元对象单独开启一个线程运行，经过规则处理，将处理后的数据传递到输出集合
4.观察输出末梢数据

 **/

class NeuronClass
{
    //输入集合，可以是神经元对象也可以是源输入源
    public $input = array();
    //输出集合，可以是神经元对象也可以是输出末梢
    private $output = array();
    //状态集合,3个元素代表3维
    private $state = array(0,1,2);


    //input函数化，方便接收数据时运行处理规则
    function input0($d)
    {
        $this->process($d,0);
    }
    function input1($d)
    {
        $this->process($d,1);
    }
    function input2($d)
    {
        $this->process($d,2);
    }

    //处理规则
    function process($d,$index){

    }
}
```

Go语言实现：



